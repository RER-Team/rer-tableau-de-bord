# OS
.DS_Store
Thumbs.db

# Node / Next (Sprint 1+)
node_modules/
.next/
out/
tsconfig.tsbuildinfo
.env
.env.local
.env.*.local
*.log
.vercel

# Dossier de transfert Vercel (secrets exportés + notes de reprise, local only)
TRANSFER/

# Skills Impeccable (outil de dev local, réinstallable via `npx impeccable skills install`)
.cursor/skills/
.github/skills/
public/uploads/*
!public/uploads/.gitkeep
.env*
